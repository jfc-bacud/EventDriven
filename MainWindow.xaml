<Window x:Class="EventDriven.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EventDriven"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="900" WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                            CornerRadius="20"  
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="2">
                            <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="MinWidth" Value="155"/>
            <Setter Property="MinHeight" Value="37"/>
        </Style>
    </Window.Resources>

    <Grid>
        <StackPanel Margin="0,0,669,0">
            <Label Content="Listify." HorizontalAlignment="Left" Height="63" Margin="20,20,0,0" VerticalAlignment="Top" Width="155" FontFamily="Constantia" FontSize="45" FontWeight="Bold"/>
            <ListBox x:Name="btnCategory" Height="215" Width="190" Margin="20,30,0,0" BorderBrush="White">
                <Button x:Name="ALLTASKS_BUTTON" Content="All Tasks" Style="{StaticResource RoundedButtonStyle}" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                <Button x:Name="My_Day_Button" Content="My Day" Style="{StaticResource RoundedButtonStyle}" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                <Button x:Name="Important_Button" Content="Important" Style="{StaticResource RoundedButtonStyle}" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                <Button x:Name="Planned_Button" Content="Planned" Style="{StaticResource RoundedButtonStyle}" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                <Button x:Name="Completed_Button" Content="Completed" Style="{StaticResource RoundedButtonStyle}" HorizontalAlignment="Left" VerticalAlignment="Top"/>
            </ListBox>
            <Button x:Name="AddCategory" Content="Add" Margin="30,50,0,0" Style="{StaticResource RoundedButtonStyle}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
        </StackPanel>

        <Frame x:Name="ListFrame" Margin="231,115,51,141" BorderBrush="Black" NavigationUIVisibility="Hidden" BorderThickness="1"/>
    </Grid>
</Window>
